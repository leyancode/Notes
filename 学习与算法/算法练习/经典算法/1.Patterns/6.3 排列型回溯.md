#algorithm/backtracking #algorithm/dfs #status/review

> [!info] 知识图谱
> 总览：[[0.0 Patterns 算法知识图谱]] · 前置：[[6.1 子集型回溯]] · [[6.2 组合型回溯]] · 原始记录：[[原始稿件/6.3.1排列形回溯|6.3.1 排列型回溯]]

# 排列型回溯

## 解决什么问题

把 $n$ 个数排成一行、**每个数恰好用一次、顺序不同算不同方案**——排列 Permutation。本质是「有 $n$ 个坑位，依次决定每个坑填谁」。和 [[6.2 组合型回溯]] 的分水岭：组合无序，每层从上一个数之后往下选；排列有序，**每层都要从头扫全部候选**，后面的数可以排在前面。方案数从 $C(n,k)$ 涨到 $n!$。

## 两种写法：候选池 vs 占用表

同一棵递归树（见 [[递归树]]），区别只在「怎么记录谁被用过」：`set` 版维护一个「还没用的数字」的候选池，布尔版维护一张「下标是否被占」的全局占用表。

> [!tip]- 核心思路：`dfs(i, s)` 前 i 坑填好，s 是剩余候选
> 
> `i` = 填到哪个坑，`s` = 还剩什么可用。递归到 `i == n` 说明坑填满，收一个答案。
> 
> - **`set` 版**直接遍历候选池 `for x in s`，选了就传 `s - {x}`——池子里只剩合法的。
> - **布尔版**遍历所有下标 `for j in range(n)`，用 `if not on_path[j]` 跳过非法的——全都看一遍，过滤掉占用的。
> 
> 结果等价。面试首选布尔版：**没有新建集合的开销**（见下方复杂度）。set 版更直观，可提一句。

> [!success]- 46. 全排列（两版对照）
> 
> ```python
> # set 版：候选池只留合法的
> class Solution:
>     def permute(self, nums: List[int]) -> List[List[int]]:
>         n = len(nums)
>         ans, path = [], [0] * n
>         def dfs(i, s):
>             if i == n:
>                 ans.append(path.copy())     # 定长 path，拷贝快照，见 [[Python_赋值与拷贝]]
>                 return
>             for x in s:                     # 枚举第 i 坑的所有填法
>                 path[i] = x
>                 dfs(i + 1, s - {x})         # s-{x} 新建集合，不改上一层，见 [[Python_集合]]
>         dfs(0, set(nums))
>         return ans
> ```
> 
> ```python
> # 布尔版：经典写法，面试更常见
> class Solution:
>     def permute(self, nums: List[int]) -> List[List[int]]:
>         n = len(nums)
>         ans, path = [], [0] * n
>         on_path = [False] * n
>         def dfs(i):
>             if i == n:
>                 ans.append(path.copy())
>                 return
>             for j in range(n):              # 排列：每层从 0 扫全部下标
>                 if not on_path[j]:
>                     path[i] = nums[j]       # path 存值，on_path 记下标，别混
>                     on_path[j] = True
>                     dfs(i + 1)
>                     on_path[j] = False      # 只还这一个，谁占谁还
>         dfs(0)
>         return ans
> ```
> 
> - 主流程：第 `i` 层只写 `path[i]` 这一格，一定先写后递归。
> - 关键变量：`path[i]` 的 `i` 是递归深度，`on_path[j]` 的 `j` 是数字在 `nums` 中的位置。
> - 易错位置：`for j in range(n)` 必须从 0 开始扫，写成 `range(i, n)` 就退化成组合了。

## 核心：为什么 `path` 不用恢复，`on_path` 却要

两个数组待遇完全不同，判断标准不是「哪个变量是共享的」，而是「哪个变量的状态**会被兄弟分支读到**」。

| 数组 | 下标含义 | 兄弟分支会撞车吗 | 要不要恢复 |
| --- | --- | --- | --- |
| `path[i]` | `i` = 递归深度 | 不会，每层独占一格 | 不用，下次进来先覆盖再用 |
| `on_path[j]` | `j` = 数字在 `nums` 的位置 | 会，整棵树共用一张表 | **必须还**，`on_path[j] = False` |

> [!tip]- 深度定位 vs 全局占用表
> 
> `path[i]` 的 `i` 就是当前层号，第 3 层永远只碰 `path[3]`，兄弟分支互不污染，而且下次进来一定先覆盖再读——所以不需要还。当 `i == n` 时，`path[0..n-1]` 全被本条路径刚写过，旧值不可能残留。这是「深度定位 + 无条件覆盖」的不变量。
> 
> `on_path` 则是整棵树共享的一张占用表。第 1 层把 `on_path[0]` 设 True，是告诉所有子孙「0 号被我占了」；子树探索完必须撤销，否则兄弟分支会误以为 0 号仍被占用，永远用不到那个数字。
> 
> 你总结过的「借了要还」在这里依然成立，只是**还的对象是 `on_path` 不是 `path`**。完整机制见 [[状态恢复]]，其中 `set` 版的 `s - {x}` 是第三种免恢复写法：每层拿一份不可变副本，从没动过上一层。

> [!example]- 推演：nums = [1, 2, 3]
> 
> ```text
> dfs(0, {1,2,3})
>  └ x=1: path=[1,_,_]  dfs(1, {2,3})
>     ├ x=2: path=[1,2,_]  dfs(2, {3})
>     │   └ x=3: path=[1,2,3]  dfs(3,{}) → 收 [1,2,3]
>     └ x=3: path=[1,3,_]  dfs(2, {2})
>         └ x=2: path=[1,3,2]           → 收 [1,3,2]
> ```
> 
> 回到 `dfs(1,·)` 的第二轮时，它手上的 `s` 依然是 `{2,3}`——`set` 版从没被改过；布尔版则是靠 `on_path` 被还原成进来时的样子。

## 例题：51. N 皇后 —— 排列的伪装

N 皇后表面在「摆棋子」，真身是「枚举列的一个排列 + 用 `r±c` 过滤对角线冲突」。整个设计的精髓：**把约束编码进数据结构，而不是编码进检查逻辑**。

> [!tip]- 第一步：换表示法，让「每行一个」免费
> 
> 二维 `board[r][c]` 允许一行塞三个皇后，非法状态它都能表示，你得自己派代码巡查。换成一维：
> 
> ```python
> col = [0] * n     # col[r] = 第 r 行的皇后放在第几列
> ```
> 
> `col[2]` 只能存一个数，**根本没能力**表示「第 2 行两个皇后」。鸽笼原理保证 n 行放 n 个必然每行恰好一个，没丢任何合法解。**第一个约束，零代码。**

> [!tip]- 第二步：列约束一来，问题退化成全排列
> 
> 加上「每列也只能一个」，即 `col` 各元素两两不同——这就是 $0 \ldots n-1$ 的一个**排列**：
> 
> $$\text{N 皇后的解} \subset 0 \ldots n-1 \text{ 的全排列}$$
> 
> 搜索空间从 $n^n$ 掉到 $n!$，而且代码跟 permute 一模一样，只在中间插一道过滤。第二个约束交给候选池 `s - {c}`，删掉用过的列。**它就是 permute，只是插了一道过滤。**

> [!success]- 51. N 皇后
> 
> ```python
> class Solution:
>     def solveNQueens(self, n: int) -> List[List[str]]:
>         ans = []
>         col = [0] * n
>         def valid(r, c):
>             for R in range(r):              # 只看已放好的第 0..r-1 行
>                 C = col[R]
>                 if r + c == R + C or r - c == R - C:
>                     return False            # 找到一个冲突就立刻否决
>             return True                     # 全部看完才通过——必须在循环外
>         def dfs(r, s):
>             if r == n:
>                 ans.append(['.' * c + 'Q' + '.' * (n - 1 - c) for c in col])
>                 return
>             for c in s:
>                 if valid(r, c):
>                     col[r] = c
>                     dfs(r + 1, s - {c})
>         dfs(0, set(range(n)))
>         return ans
> ```
> 
> - 骨架就是 permute：`col[r]=c` 对应 `path[i]=x`，`dfs(r+1, s-{c})` 对应 `dfs(i+1, s-{x})`。
> - `valid` 的不变量：`col[0..r-1]` 是有效数据，`col[r..n-1]` 是残留垃圾，但只循环 `range(r)`，永远读不到垃圾区——和 `path` 不用清理同一件事。
> - `return True` 是**关于全体的断言**，必须等全体看完，只能放循环外；放循环里等于看第一个人就宣布「全场没人反对」。`r=0` 时循环体一次没进，正确答案 `True` 恰好由循环外的兜底提供。

> [!tip]- 对角线为什么是加减法：几何问题代数化
> 
> 同一条对角线上的格子坐标满足恒等式。沿 ↘ 走一步行 +1 列 +1，`r - c` 不变；沿 ↙ 走一步行 +1 列 -1，`r + c` 不变。于是「两皇后同对角线」这个**几何**问题变成「两个数字相等」这个**代数**问题：
> 
> ```python
> if r + c == R + C or r - c == R - C:    # r+c 和 r-c 是格子的两个「门牌号」
>     return False
> ```
> 
> 等价于常见的 `abs(r-R) == abs(c-C)`（行差等于列差），但直接比身份证号更省事。

> [!tip]- 为什么按行推进：决策顺序规范化去重
> 
> `dfs(r)` 一层管一行，r 从 0 到 n-1。若「想放哪放哪」，先放 (0,1) 再 (1,3) 和先放 (1,3) 再 (0,1) 是同一棋盘，同一个解会被数很多遍。固定「第 r 层只决定第 r 行」，每个解在递归树里对应唯一一条路径——去重不靠事后过滤，靠**决策顺序规范化**从源头杜绝。和子集型「第 i 层只决定第 i 个元素选不选」同理。

## 三个约束的分工

| 约束 | 由谁保证 | 代价 |
| --- | --- | --- |
| 每行一个 | 数据结构 `col` 的形状 | 零代码 |
| 每列一个 | 候选池 `s - {c}` | 零额外检查 |
| 对角线 | `valid()` 显式检查 | $O(r)$ |

三个约束，两个被「设计掉」，只剩一个真检查。**好的搜索算法，一大半功夫花在让约束自动满足上，而不是花在检查上。**

## 易错点

> [!warning]- 易错点
> 
> - **忘记 `on_path[j] = False`**：最高频错误。后果不是崩溃，而是答案严重变少——每个数字全局只用一次，最后只剩 1 个（甚至 0 个）排列。
> - **恢复写在 `if` 外面**：`on_path[j] = False` 必须严格包在 `if not on_path[j]` 内部、跟 `= True` 配对；写到外面会把别的分支占的位也清掉。谁占谁还，见 [[状态恢复]]。
> - **`path[i] = j` 写成下标**：要填的是 `nums[j]`（值），`on_path` 记的才是下标，两者别混。
> - **`for j in range(i, n)`**：排列不是组合，每层必须从 0 扫全部下标，后面的数可以排在前面；写成 `range(i, n)` 就变子集/组合了。
> - **`ans.append(path)` 忘 copy**：存引用不存快照，见 [[Python_赋值与拷贝]]。
> - **N 皇后 `return True` 放进循环**：全称断言必须等全体看完，只能在循环外。

## 复杂度

> [!summary]- 复杂度
> 
> | 写法 | 时间 | 空间（不含答案） |
> | --- | --- | --- |
> | set 版 | $O(n^2 \cdot n!)$ | $O(n^2)$ |
> | 布尔版 | $O(n \cdot n!)$ | $O(n)$ |
> 
> 叶子共 $n!$ 个。布尔版每层是 $O(n)$ 扫描但**不复制任何东西**，栈上只有 $n$ 个整数帧；set 版每层要新建一个 $O(n)$ 集合，深度 $n$ 层就挂了 $O(n^2)$ 的集合。N 皇后再叠加 `valid` 每次 $O(r)$，但被对角线过滤掉的分支远多于 $n!$，实际远快于上界。

## 一句话总结

排列 = 逐位填坑 + 一张全局占用表：`path` 靠「深度定位、无条件覆盖」自动保持干净，`on_path` 靠「谁占谁还」手动维持不变量；N 皇后不是摆棋子，是枚举列的排列再用 `r±c` 过滤对角线。

## 知识图谱

- 前置：[[6.0 回溯型算法]] · [[6.1 子集型回溯]] · [[6.2 组合型回溯]]
- 基础概念：[[Python_集合]] · [[状态恢复]] · [[递归树]] · [[剪枝]] · [[Python_赋值与拷贝]]
- 推荐练习：46. 全排列、51. N 皇后、47. 全排列 II、52. N 皇后 II

## 模板归纳

**触发信号**：

- 「所有排列」「顺序不同算不同方案」「每个数用一次」——排列的身份证
- 「每行/每列/每个位置恰好一个、两两不同」——本质是排列（N 皇后、数独）
- 需要一张「谁被用过」的全局表，且这张表会被兄弟分支读到 → 用完必须还原

**落笔步骤**：定长 `path` + `on_path`（或 `set` 候选池）→ `dfs(i)`，`i == n` 收答案 → `for j in range(n)` 从 0 扫全部，`if not on_path[j]` 过滤 → 写 `path[i]`、置 `on_path[j]=True`、递归、`on_path[j]=False` 还原。约束型（N 皇后）先想「能不能把约束编码进数据结构」，剩下的才用 `valid` 检查。
