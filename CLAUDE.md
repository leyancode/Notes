# CLAUDE.md

这是一个 Obsidian 知识库仓库，不是软件项目。默认不要运行构建、测试或格式化工具。

## 作用域（重要）

本仓库同时存放笔记和个人内容。**在 `notes/ampp` 分支上工作时，只允许触碰以下两个目录**：

| 目录 | 权限 |
| --- | --- |
| `OneNote/postgraduate/Advanced Message-Passing Programming/` | **只读**。OneNote 导出的原始稿，任何情况下都不要修改、移动或删除 |
| `学习与算法/课堂笔记/AMPP/` | **读写**。整理后的正式笔记写在这里 |

唯一的例外：`OneNote/postgraduate/Advanced Message-Passing Programming/提示词-Obsidian整理.md` 可写，用于更新末尾的进度表。

**其余任何目录都不要读取、修改或提交**，特别是 `日常随笔与灵感/`、`求职/`、`健身习惯/`、`金融理解/`、`日常规划与调整/`、`草稿/`。这些是个人内容，与本任务无关。如果任务看起来需要动到作用域之外的文件，停下来问，不要自行扩大范围。

## 工作方式

- 始终在 `notes/ampp` 分支上工作，**不要直接提交到 `main`**，不要合并到 `main`。合并由仓库所有者审阅 diff 后手动进行。
- 整理任务的完整规则写在 `OneNote/postgraduate/Advanced Message-Passing Programming/提示词-Obsidian整理.md`，动笔前先读它，严格按里面的格式约束执行。
- 参考已完成的样板：`学习与算法/课堂笔记/AMPP/07 单边通信与 RMA.md`。结构、密度、折叠粒度以它为准。

## 提交约定（进度追踪依赖这个）

**一讲一个 commit，做完立刻提交并推送。** 不要攒着几讲一起提交——云端会话中断时未提交的改动会全部丢失。

每个 commit 必须包含两部分：

1. 新建或修改的笔记文件
2. `提示词-Obsidian整理.md` 末尾进度表中对应行的状态更新（`待整理` → `✅ 已完成`）

commit message 格式：

```
notes(AMPP): 整理 T6 → 06 邻域集体通信
```

这样仓库所有者用一条命令就能看到全部进度：

```bash
git log --oneline main..notes/ampp -- "学习与算法/课堂笔记/AMPP"
```

## Obsidian 格式硬约束

违反这两条会让笔记在 Obsidian 里直接显示错误，输出前必须逐条自检：

1. **Callout 正文必须换行**。`> [!type]- 标题` 独占一行，正文另起一行以 `> ` 开头，中间隔一个 `> ` 空行。正文挤在标题行后面会导致折叠失效。唯一例外是顶部不折叠的 `> [!info] 知识图谱`。
2. **本库启用了 Dataview**，它会劫持两种语法：反引号代码段不能以 `=` 开头（`` `==` `` 会报 `PARSING FAILED`，改用 `$==$` 或补全为完整表达式）；正文不能出现裸露的 `::`（包进反引号，如 `` `std::vector` ``）。

完整的格式规则、断图重建策略、折叠粒度和自检清单见 `提示词-Obsidian整理.md`。
